# Procedure

## Prerequisites

- [ ] Read `docs/STATE.md`, `docs/DEBUGGING.md`, and `experiments/EXPERIMENT_LOG.md`.
- [ ] For Pokemon GO login work, also read `docs/LOGIN_READ_FIRST.md`.
- [ ] Record the device/AVD, serial, Android API/build, app version, root state, and network.

## Steps

1. Capture the current state before changing anything.
2. Change exactly one variable, or mark the attempt as read-only.
3. Run the command/action with a bounded timeout.
4. Preserve raw logs, screenshots, and reports under `artifacts/`.
5. Record exit codes and observed UI/device state.

## Commands Used

```powershell
# exact commands go here
```

## Configuration Applied

```json
{}
```
