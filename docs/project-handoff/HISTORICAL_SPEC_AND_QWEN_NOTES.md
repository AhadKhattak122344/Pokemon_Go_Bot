# Historical specification and Qwen notes

The supplied document contains proposals for a fleet API, template VM 9000,
Bliss OS guests, persistence/queues/metrics and authorized instrumentation. It
also explicitly retracts prior claims of guaranteed compatibility, stealth,
root concealment and ready-to-deploy status. Those are archival context.

The complete attributed transcript is
[PROJECT_HANDOFF_SOURCE.md](../../archive/historical-proposals/PROJECT_HANDOFF_SOURCE.md).
The earlier recipe was separately checked in
[HANDOFF-FACT-CHECK.md](../HANDOFF-FACT-CHECK.md).

Bliss release numbers must not be treated as Android API/release numbers. Verify
`ro.build.version.sdk` and `ro.build.version.release` on the actual selected image.
The transcript's unverified guest-version descriptions are not configuration defaults.
