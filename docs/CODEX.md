# Codex workflow

Read STATE.md, DEBUGGING.md and MODEL_STRATEGY.md before each task. Inspect Git status; checkpoint before structural edits. Preserve working behavior and downloaded assets. Use the harness every time. Update the experiment log after each attempt, including failures and inconclusive results. Never promote user proposals or historical observations into live test results.

# Fresh worker brief

- Objective / exact bounded decision:
- Role, exact model and effort:
- Owned files (or read-only evidence paths):
- Current facts and observed failure:
- Evidence paths and relevant command exit codes:
- Attempted fixes and what changed after each:
- Constraints: no delegation; no device/VM/disk operations; preserve other edits.
- Acceptance checks / condition:
- If escalating medium to high, the concrete unresolved reasoning obstacle:
- Return: paths, outcome, checks/exit codes, uncertainty and next action;
  at most 200 words (300 for difficult diagnosis), no raw logs.
