# Project handoff - Android Cloud Lab / Pokemon GO investigation

Updated: 2026-09-14 EDT.

This handoff is the current entry point for the whole checkout at
`C:\Users\ahadk\Downloads\Pokemod_Qwen\Pokemon_Go_Bot`. It summarizes what the
project is, what has been completed, what has failed, where the evidence lives,
and what a new session must read before touching devices or experiments.

## Current bottom line

The repository now has a working Python CLI, Windows emulator lifecycle scripts,
diagnostics, observation, experiment capture, Proxmox plan/preflight/deploy code,
experiment memory, and regression tests. The local API 36 emulator can boot and
open Pokemon GO 0.427.0, but the designated Google account still gets the game's
`Failed to Sign In` dialog. API 36.1 and API 37 official Google Play images do
not currently provide a usable display on this host; API 37 software mode boots
to the Android boot marker but SurfaceFlinger crashes during screen capture.

No successful Pokemon GO playable-map login has been achieved on the emulator.
No live Proxmox host has been contacted. No API 37 game login was attempted
because display readiness failed first.

## Repository identity

- Project package: `android-cloud-lab`
- Installed command: `lab`
- Python entry point: `android_lab.cli:main`
- Main language: Python 3.11+ with PowerShell helpers on Windows
- Dependency manager: `uv`
- Test framework: `pytest`
- Runtime artifact directory: ignored `artifacts/`
- Local Android SDK/tool cache: ignored `.tools/`
- Local virtualenv/cache: ignored `.venv/`, `.uv-cache/`
- Active app module: none. The old recovered Android/Gradle material is retained
  only under `archive/` as provenance/reference.

## Required read order for a future session

Read these before making changes or running another experiment:

1. `AGENTS.md`
2. `docs/STATE.md`
3. `docs/DEBUGGING.md`
4. `docs/CODEX.md`
5. `docs/MODEL_STRATEGY.md`
6. `docs/LOGIN_READ_FIRST.md` before any Pokemon GO login attempt
7. `experiments/EXPERIMENT_LOG.md` before any retry or new experiment
8. `codex_memory/current_context.md`
9. `codex_memory/attempted_approaches.json`
10. `knowledge_base/known_failures.md`

Before proposing or executing an approach, run:

```powershell
uv run python tools/validate_against_history.py "short description of proposed approach"
```

A nonzero exit means the idea matches a known non-retryable failure; read the
reported source before proceeding.

## Project directory

Meaningful top-level layout:

```text
Pokemon_Go_Bot/
  .codex/                 Project Codex agent role files.
  .tools/                 Ignored local Android SDK/emulator installation.
  .uv-cache/              Ignored local uv cache used for sandbox-friendly syncs.
  .venv/                  Ignored local Python environment.
  android_lab/            Active Python implementation.
  archive/                Historical/recovered material; reference-only.
  artifacts/              Ignored runtime logs, screenshots, reports, APKs.
  assets/                 Preserved local assets and manifest.
  codex_memory/           Compact machine-readable/current session memory.
  config/                 YAML scenarios, routes, Docker and Proxmox configs.
  docs/                   Maintained documentation and this handoff.
  experiments/            Dated tracked findings and experiment ledger.
  knowledge_base/         Summaries of verified, failed and deprecated paths.
  tests/                  Unit tests and regression tests.
  tools/linux/            Docker/Linux emulator helper scripts.
  tools/windows/          Native Windows SDK, AVD and emulator scripts.
  README.md               Main project overview.
  START-HERE.md           First-run Windows setup.
  pyproject.toml          Python package/test configuration.
  uv.lock                 Locked dependencies.
```

Active source modules:

```text
android_lab/
  adb.py                  Bounded ADB subprocess transport and screenshots.
  cli.py                  lab command parser and dispatch.
  config.py               YAML/default config loading.
  default.yaml            Packaged default scenario.
  diagnostics.py          Read-only logcat/screenshot/package captures.
  emulator.py             Docker compose lifecycle wrapper.
  experiment.py           Pre-capture, optional launch and login observation.
  health.py               Device readiness probes.
  location.py             Emulator geolocation helpers.
  observe.py              Timed process/activity/screenshot observer.
  proxmox.py              Proxmox plan/preflight/deploy implementation.
  root.py                 Explicit debug-ADB root/unroot controls.
```

Important configs:

```text
config/default.yaml       Default Docker/API34 scenario.
config/api36.yaml         Native clean API36 baseline scenario.
config/pokemongo.yaml     API36 Pokemon GO scenario, emulator-5556.
config/pokemongo-api361.yaml
                           API36.1 Pokemon GO scenario, emulator-5558.
config/pokemongo-api37.yaml
                           API37 Pokemon GO scenario, emulator-5560.
config/proxmox.example.json
                           Example Proxmox config.
config/proxmox.json       Ignored/local Proxmox config; currently placeholders.
config/docker/            Docker emulator definitions.
config/routes/            Sample GPX route.
```

Important Windows scripts:

```text
tools/windows/Android-Environment.ps1
tools/windows/Bootstrap-Android.ps1
tools/windows/Emulator-Profiles.ps1
tools/windows/Start-Emulator.ps1
tools/windows/Stop-Emulator.ps1
tools/windows/Test-Emulator.ps1
tools/windows/Test-Profiles.ps1
tools/windows/Run-Experiment.ps1
tools/windows/Install-Magisk.ps1
tools/windows/Root-Emulator.ps1
tools/windows/install_magisk.py
```

Important tracked knowledge/docs:

```text
docs/STATE.md
docs/DEBUGGING.md
docs/COMMANDS.md
docs/ARCHITECTURE.md
docs/CODEX.md
docs/MODEL_STRATEGY.md
docs/LOGIN_READ_FIRST.md
docs/INSTALLED_COMPONENTS.md
experiments/EXPERIMENT_LOG.md
experiments/2026-09-09-fresh-login-investigation.md
experiments/2026-09-13-live-integration.md
experiments/2026-09-13-shungo-installed.md
knowledge_base/verified_working.md
knowledge_base/known_failures.md
knowledge_base/assumptions.md
knowledge_base/deprecated.md
codex_memory/current_context.md
codex_memory/attempted_approaches.json
codex_memory/decision_log.md
```

## Git/worktree status at handoff

The worktree is intentionally dirty with many already-created project changes.
Do not revert unrelated edits. Current status includes modifications/additions in:

- `README.md`
- `android_lab/cli.py`
- `android_lab/experiment.py`
- `android_lab/observe.py`
- `config/pokemongo.yaml`
- `config/pokemongo-api361.yaml`
- `config/pokemongo-api37.yaml`
- `docs/ARCHITECTURE.md`
- `docs/COMMANDS.md`
- `docs/DEBUGGING.md`
- `docs/LOGIN_READ_FIRST.md`
- `docs/STATE.md`
- `docs/HANDOFF_2026-09-14.md`
- `experiments/2026-09-09-fresh-login-investigation.md`
- `experiments/2026-09-09-session-actions.txt`
- `experiments/2026-09-13-live-integration.md`
- `experiments/2026-09-13-shungo-installed.md`
- `experiments/EXPERIMENT_LOG.md`
- `tests/unit/test_experiment.py`
- `tests/unit/test_handoff.py`
- `tests/unit/test_history_validator.py`
- `tests/unit/test_observe.py`
- `tools/verify_repository.py`
- multiple `tools/windows/*.ps1` and `tools/windows/install_magisk.py`
- untracked `codex_memory/`, `knowledge_base/`, `experiments/TEMPLATE/`,
  `tools/validate_against_history.py`

Preserve ignored downloaded assets and runtime outputs. Do not clean `.tools/`,
`.venv/`, `.uv-cache/`, `artifacts/`, or existing AVD data as housekeeping.

## Implemented and verified work

CLI and package:

- `lab` resolves to `android_lab.cli:main`.
- Commands include `status`, `diagnostics`, `observe`, `experiment`,
  `install`, `location`, `root`, and `proxmox plan/preflight/deploy`.
- `lab diagnostics` performs read-only capture and exits nonzero when required
  artifact collection is incomplete.
- `lab observe` streams timestamped process/activity/screenshot samples and
  records completeness in `observe.json`.
- `lab experiment` captures before optional launch, refuses concealment/integrity
  zip-style inputs, and distinguishes launch/capture/authentication evidence.
- `root` commands are explicit debug-ADB transitions, not Magisk/root concealment.

Windows emulator lifecycle:

- Stable profiles exist for:
  - `Api36`: `poke_api36_test`, `emulator-5556`, Android 16/API 36.
  - `Api361`: `poke_api361_test`, `emulator-5558`, Android 16/API 36.1.
  - `Api37`: `poke_api37_test`, `emulator-5560`, Android 17/API 37.
  - `Play`: `baseline`, `emulator-5554`, API 34 Google Play.
  - `Rooted`: `baseline-rooted`, `emulator-5554`, API 34 Google APIs.
- `Start-Emulator.ps1` supports GPU overrides and tracks only its launched
  process tree for timeout cleanup.
- Cleanup now validates process creation times to avoid killing PID-reused or
  older orphan processes.
- `Test-Profiles.ps1` covers profile parsing, syntax, process-tree cleanup,
  PID reuse and timestamp precision behavior.

Proxmox:

- `android_lab/proxmox.py` implements offline planning, read preflight and
  deploy journaling for QEMU template clones.
- Unit tests mock API/task behavior, task ordering, auth/TLS, timeouts and
  failure paths.
- Current local `config/proxmox.json` still points to:
  - host `https://pve.example.com:8006`
  - node `pve`
  - template VMID `9000`
  - storage `local-lvm`
  - bridge `vmbr0`
- No real Proxmox host has been contacted; all Proxmox success is code/offline
  planning/mocked behavior only.

Experiment memory:

- `tools/validate_against_history.py` checks proposed approaches against
  `codex_memory/attempted_approaches.json`.
- Known non-retryable paths are recorded for API34 translation SIGILL, unchanged
  API36 same-account login retries, API36.1 display failure, API37 auto/host
  boot failure, API37 software display failure, Docker/WSL without KVM, and
  archived/root-concealment/identity-bypass stacks.

## Validation status

Latest completed validation:

```text
uv run pytest -p no:cacheprovider --basetemp artifacts/tmp/pytest-live-20260913
134 passed in 0.84s

tools/verify_repository.py
PASS: 280 archive hashes, 30 documentation files, 15 CLI help paths

tools/windows/Test-Profiles.ps1
passed after mocked cleanup/PID-reuse/timestamp precision fixes

git diff --check
exit 0, with existing line-ending notices only
```

Evidence:

- `artifacts/test-resume-final2-20260914.log`
- `artifacts/verify-resume-final-20260914.log`
- `artifacts/diff-resume-final-notices.log`
- `artifacts/api37-timeout-cleanup-result.json`

The tests prove repository behavior and mocked fleet logic. They do not prove
Pokemon GO login, Play certification, live Proxmox deployment or Android guest
app acceptance.

## Device and app findings

API 36 clean baseline:

- AVD: `poke_api36_test`
- Serial: `emulator-5556`
- Android: 16/API 36
- Root state: clean ADB UID 2000; no Magisk patches applied.
- Game: Pokemon GO 0.427.0 opens and reaches splash/account flows.
- Failure: designated Google account selection returns Pokemon GO `Failed to
  Sign In`; process remains running; crash buffer empty.
- Play Store UI showed `Device is not certified`; its repair flow failed.
- User confirmed the same game account works on a physical iPhone, so this is
  not a generic account-login failure.
- Next meaningful comparison is a certified physical Android with the same
  designated account/network, or publisher-side transaction diagnosis.

API 34 rooted/debug:

- AVDs: `baseline` / `baseline-rooted`, `emulator-5554`
- Magisk `su` was reported/verified on the modified API34 debug image.
- Pokemon GO ARM64 split on rooted API34 crashes with SIGILL in `UnityMain`
  after `ndk_translation` starts.
- Do not repatch API34 or try root hiding to fix this translation crash.

API 36.1 official Google Play:

- AVD: `poke_api361_test`
- Serial: `emulator-5558`
- Image: `system-images;android-36.1;google_apis_playstore;x86_64`, revision 4.
- Auto/host boot failed with SurfaceFlinger crash.
- Software mode reached boot marker, but screenshot capture failed.
- Disabling shared slots was accepted but failed with the same assertion.
- No game login was attempted because display readiness failed.

API 37 official Google Play:

- AVD: `poke_api37_test`
- Serial: `emulator-5560`
- Image: `system-images;android-37.0;google_apis_playstore;x86_64`, revision 6.
- Emulator: 37.1.11
- Auto/host rendering: ADB stayed offline through startup timeout.
- Software rendering: reached boot marker and profile checks, but display capture
  failed with SurfaceFlinger SIGABRT:
  `Assertion failed: !rcEnc->featureInfo()->hasReadColorBufferDma`.
- Software plus `ANDROID_EMULATOR_FEATURES=-GLDirectMem`: same failure.
- User-requested one rerun on 2026-09-14: same display failure, no PNG screenshot,
  same SurfaceFlinger assertion. Stop helper completed successfully afterward.
- No game login, account addition, root, image patch or Proxmox operation was
  performed on API37.

Shungo:

- Official Shungo 1.7.0 APK was downloaded from the official source, verified
  with `aapt`/`apksigner`, installed on API36 and logged in by the user.
- Dashboard loads, but Start is disabled because the subscription is not
  recognized/available.
- Overlay permission remains off; an automatic approval review rejected a prior
  attempt to grant it.
- Static inspection found WebView/Auth0/dashboard/foreground service/root shell
  and external injector paths. Actual injector/game-hook behavior is unverified.

## Key experiment ledger

- `V01`: API36 native WHPX boot/readiness succeeded.
- `V02`: CLI `status` and `diagnostics` succeeded on API36.
- `V03`: Proxmox offline plan succeeded; no host contacted.
- `V04`: Pokemon GO 0.427.0 opens on clean API36.
- `F01`: API34 rooted image hits ARM64 translation SIGILL in Pokemon GO.
- `F02/F03`: API36 reaches login path but game returns `Failed to Sign In`;
  Play Store says not certified.
- `V08`: live observer implemented; API36 game reached Google account chooser.
- `V09`: Shungo installed and user login succeeded; subscription blocks Start.
- `L10-L15`: warm/cold API36 login observations and Play Store listing check.
- `L16-L17`: designated account selected; `Failed to Sign In` reproduced.
- `L18-L22`: API36.1 and API37 images installed/provisioned; display/boot failures.
- `L23`: API37 software boot marker reached, display failed.
- `L24`: API37 software with GLDirectMem disabled, display failed.
- `T01`: emulator startup timeout cleanup fixed and verified.
- `L25`: user-requested unchanged API37 software rerun, display failed again.
- `M01/M02`: experiment memory/knowledge base added; bypass runbook recorded as
  rejected, not implemented.

The authoritative details and artifact paths are in `experiments/EXPERIMENT_LOG.md`.

## Important artifact paths

Recent API37 one-run result:

```text
artifacts/api37-once-start.log
artifacts/profile-Api37-3edf9296c8d5495a8b11e1f76698be32.json
artifacts/api37-test/once/report.json
artifacts/api37-test/once/crash.txt
artifacts/api37-test/once/logcat.txt
artifacts/api37-test/once/screen.raw
artifacts/emulator-poke_api37_test-b7eb3b970ac4498395dbc4b81efc74b1.log
artifacts/api37-once-stop.log
```

Prior API37/API36.1 display failures:

```text
artifacts/api37-test/resume-before/
artifacts/api37-test/no-directmem/
artifacts/api361-test/first-boot/
artifacts/api361-test/software-boot/
artifacts/api361-test/software-ready/
artifacts/api361-test/sharedslots-disabled/
```

API36 login/designated account:

```text
artifacts/pgo-designated-account-before/
artifacts/pgo-designated-account-selected/
artifacts/pgo-designated-account-result/
artifacts/login-account.json
```

Shungo:

```text
artifacts/shungo-20260913/
```

Repository validation:

```text
artifacts/test-resume-final2-20260914.log
artifacts/verify-resume-final-20260914.log
artifacts/diff-resume-final-notices.log
artifacts/api37-timeout-cleanup-result.json
```

Keep raw/private data inside ignored `artifacts/`. Do not move account data or
raw logs into tracked docs.

## Current blockers

1. Pokemon GO emulator login is unresolved. The strongest local baseline is API36:
   the game launches, the designated account fails at the game sign-in step, and
   Play Store reports the device is not certified.
2. API36.1 and API37 official images are not usable on this host because display
   capture crashes SurfaceFlinger. Boot markers are not enough.
3. Proxmox cannot proceed without real host/node/template/storage/network values.
4. Shungo cannot start automation without recognized subscription/entitlement,
   overlay permission approval, compatible rooted runtime, and successful game
   login.
5. No tested Android guest currently satisfies the requested playable Pokemon GO
   acceptance condition.

## Do not repeat without new evidence

- Do not rerun unchanged API37 software or GLDirectMem-disabled settings as a
  repair; L23, L24 and L25 already failed display acceptance.
- Do not rerun API36.1 auto/software/shared-slots renderer variations unchanged.
- Do not patch/root the clean API36 baseline to guess at sign-in.
- Do not repatch API34 for the known translation SIGILL.
- Do not cycle Google accounts; the designated account has already been selected.
- Do not treat Google account picker, splash screen, `boot_completed=1`, or
  resumed activity as successful game login.
- Do not execute archived identity/concealment/integrity scripts.
- Do not call mocked Proxmox tests or offline plans a live deployment.
- Do not use Docker/WSL software emulation again without proof usable KVM exists.
- Do not infer Play certification from Google packages or security email labels.

## Next useful actions

Best next technical path depends on available hardware:

1. If a certified physical Android is available, run one official Pokemon GO login
   attempt with the designated account and same network, capture screenshots/logs,
   and compare against the emulator failure.
2. If Proxmox is the next target, replace placeholder `config/proxmox.json` values
   with a real host, node, template VMID, storage and bridge, then run
   `lab proxmox plan` and `lab proxmox preflight` before any deploy.
3. If continuing local emulator graphics research, use only a new renderer/image
   hypothesis backed by host/emulator evidence; require screenshot PNG success
   before any game login attempt.
4. If using Shungo, resolve the subscription/entitlement and get explicit overlay
   approval before trying to start it. It still needs a compatible rooted runtime
   and a game login path.

## Commands a future operator will likely need

Repository checks:

```powershell
$env:UV_CACHE_DIR=(Resolve-Path .uv-cache).Path
uv sync --extra test --offline
uv run pytest -p no:cacheprovider
uv run python tools/verify_repository.py
git diff --check
```

API36 baseline:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools/windows/Start-Emulator.ps1 -Profile Api36 -TimeoutSeconds 180
uv run lab --config config/pokemongo.yaml diagnostics --package com.nianticlabs.pokemongo
uv run lab --config config/pokemongo.yaml observe --duration 30 --interval 3
powershell -NoProfile -ExecutionPolicy Bypass -File tools/windows/Stop-Emulator.ps1 -Profile Api36
```

API37 display-only validation, only if there is a genuinely new hypothesis:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File tools/windows/Start-Emulator.ps1 -Profile Api37 -Gpu software -TimeoutSeconds 180
uv run lab --config config/pokemongo-api37.yaml diagnostics --out artifacts/api37-test/<new-name>
powershell -NoProfile -ExecutionPolicy Bypass -File tools/windows/Stop-Emulator.ps1 -Profile Api37
```

Proxmox offline review:

```powershell
uv run lab proxmox plan --config config/proxmox.json --out artifacts/proxmox-plan.json
uv run lab proxmox preflight --config config/proxmox.json --out artifacts/proxmox-preflight.json
```

## Final state of the latest user-requested run

The user asked: `ok run it once`.

That exact single run was performed against API37 software rendering. Android
reported boot/version success, but diagnostics failed because the device did not
return a PNG screenshot. Crash logs show the same SurfaceFlinger
`hasReadColorBufferDma` assertion. The emulator was then stopped with
`Stop-Emulator.ps1 -Profile Api37`; the stop log says `OK: killing emulator, bye bye`.

This confirmed another API37 display failure, not a game-login result.
