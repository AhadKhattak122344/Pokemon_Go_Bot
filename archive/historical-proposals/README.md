# Superseded proposals

Preserved to avoid discarding the previous uncommitted work. These scripts and
claims are not exposed by the current CLI and are not used in setup or tests.
They include unsupported root concealment, fingerprint and integrity-module
assumptions that the supplied September 9 handoff explicitly excludes.

Use `lab diagnostics`, debug-root controls for owned debuggable AVDs, and
`lab proxmox` for the maintained QA workflow. `PROJECT_HANDOFF_SOURCE.md` is an
attributed text extraction, including historical claims; maintained decisions live
under `docs/project-handoff/`.
